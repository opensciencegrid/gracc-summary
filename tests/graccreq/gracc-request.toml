[AMQP]

# Host information
host = "localhost"
vhost = "/"

# Username and password for the AMQP router
username = "guest"
password = "guest"

# Exchange and queue to reqeuest summaries from
exchange = 'gracc.osg.requests'
queue = 'gracc.osg.requests'

[ElasticSearch]
raw_index = 'gracc.osg.raw0-*'
raw_type = 'JobUsageRecord'

